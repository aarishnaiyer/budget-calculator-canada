[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    smoke: quick sanity checks
    calculation: math and output correctness
    ui: visual and layout tests
    edge_case: boundary and unusual inputs
